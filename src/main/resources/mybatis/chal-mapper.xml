<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Chal">
	<select id="listBound" resultType="kh.spring.dto.ChalBasicDTO">
		select * from(select challenge.*, row_number() over(order by seq asc)rn from challenge) join chalimg using(seq) where rn between #{start} and #{end}
	</select>
</mapper>